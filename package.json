{
  "name": "hermrouter",
  "version": "1.4.0",
  "description": "Self-hosted LLM proxy — 14-dimension classifier routes requests to the right model automatically",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "node dist/src/server.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npx tsx test/test-router.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.2.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
